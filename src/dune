(rule
  (enabled_if (>= %{ocaml_version} "5.0"))
  (target StdlibShim.ml)
  (action (copy StdlibShim.5.ml StdlibShim.ml)))

(rule
  (enabled_if (< %{ocaml_version} "5.0"))
  (target StdlibShim.ml)
  (action (copy StdlibShim.4.ml StdlibShim.ml)))

(library
 (name Algaeff)
 (public_name algaeff))
